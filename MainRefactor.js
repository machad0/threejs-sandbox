class MainCtrl {
  constructor (camera, controls, scene, renderer, height, width, rendererIframe, sphere) {
    this.camera = camera
    this.controls = controls
    this.scene = scene
    this.height = height
    this.width = width
  }
  init () {
  }
}
module.exports = MainCtrl
